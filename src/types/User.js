/**
   * @typedef {object} User
   * @property {string}id
   * @property {string} name
   * @property {Blob | null} image
   * @property {string || null} email
   * @property {'local' | online} type
   */
